package soundcode.engine

import soundcode.domain.Note

object TimeScheduler {
  // Metodo "fantoccio": dovrebbe calcolare il tempo totale, 
  // ma per ora restituisce sempre 0 di proposito.
  def calculateTotalTime(notes: List[Note]): Int = {
    0
  }
}
